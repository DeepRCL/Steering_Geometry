"""Schwartz value-steering pipeline on top of COLD-Steer.

Supports ``cold_fd`` (LossFDSteerer) and ``cold_kernel`` (KernelLossSteerer).
Mirrors ``llm-steering-opt/pipeline`` and ``odesteer/scripts/schwartz``.
"""
